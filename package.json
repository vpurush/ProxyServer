{
  "name": "ProxyServer",
  "version": "1.0.0",
  "description": "Anonymous proxy server",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "vpurush",
  "license": "ISC",
  "dependencies": {
    "request": "^2.51.0"
  }
}
